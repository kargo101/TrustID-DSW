TrustID Peak Final Polished

This build keeps the original TrustID business logic and the approved PremiumForm artwork.

Changes made:
- Reworked the UI styling so cards, buttons, inputs and navigation match the background artwork.
- Kept original control positions on the main screens to avoid overlap.
- Rebuilt the Login layout and kept Forgot Password and Show Password.
- Added Show Password to Create Account and Settings.
- Kept profile-photo upload.
- Simplified header wording so text fits cleanly.
- Login now accepts username or email.
- Prevented duplicate email addresses when editing Settings.
- Restored the saved Joined Date when accounts are loaded.
- Verification approve/reject now saves the user's account state.
- Removed two unused Create Account fields that caused warnings.
- Removed machine-specific user_dashboard.csproj.user.
- Removed stale bin, obj and .vs folders.

No new comments were added to the new UI code.
